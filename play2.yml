---
- name: Create group and user
  hosts: localhost
  become: yes
  gather_facts: no
  tasks:
    - name: Create group
      group:
        name: group1
        state: present
    
    - name: Create user
      user:
        name: user1
        state: present